import ControlledCarousel from 'components/carousel';
import React from 'react';

function Homepage() {
    return (
        <div>
            <ControlledCarousel />
        </div>
    );
}

export default Homepage;
